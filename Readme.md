> Porfolio con DOM
**Actividad 19**

# Manipulación del DOM 

![imagen_de_portada](https://www.paginaswebs.com/wp-content/uploads/2017/01/dom-document-object-model-rimorsoft-online.jpg)

En esta actividad vamos crear un portfolio interactivo gracia a la manipulación del DOM con JavaScript.

## Objetivos 

- Crear un formulario con 3 campos: **título**, **src** y **description**.
- Recoger la información del formulario y **disponer un grid de tarjetas creadas a partir de esa información**.
- Trabajar en los estilos CSS para que quede lo más profesional posible.
- Subir nuestra versión a [GitHub](https://github.com/)


---

### **Iniciamos la actividad** 

1. Entrar en la carpeta donde vamos a ubicar la actividad:
    1. Abre la **terminal**.
    2. Con los comandos `cd` y `cd ..` navegamos por nuestro ordenador hasta llegar a la dirección dónde queremos ubicar la actividad.

2. Abrir en enlace de la actividad en el navegador.
3. Clonar la actividad:
    1. Copiamos el link *https* del botón *Clone* de nuestro **Github**.
    2. Vamos a la **terminal** y, dentro de la carpeta donde queremos ubicar la actividad, escribimos el comando `git clone + (pegamos el link que habíamos copiado)`.

4. Abrir la actividad en el **Visual Studio Code**
    1. Entramos dentro de la carpeta que se ha generado con el comando `git clone`, haciendo `cd + (el nombre de la carpeta clonada)`.
    2. Dentro de la carpeta, escribimos el comando `code .` para abrir esta carpeta en el **VSC**.

---


#### :raised_hand::warning: Antes de empezar :point_down: 

Este proyecto tiene **Tailwind** 🌈✨, pero puedes utilizar CSS puro si así lo prefieres.
*No necesitas instalar nada ni compilar*
---

### :point_right: **Ejercicio** 

- Creamos los elementos estáticos necesarios en **HTML**. (por ejemplo: un título, un div con display grid y un formulario con 2 inputs y un textarea.)

- Pensamos en qué **estructuras de almacenamieto** de datos serían las más idóneas para cumplir con nuestro objetivo **(arrays, objetos)**.

- *Almacenamos* la **información** recogida con el **formulario**.

- Creamos las **tarjetas** con la información recogida dinámicamente y las añadimos dentro del **grid**.

---

### **Mientras trabajamos en la actividad**

Mientras trabajamos en la actividad es muy importante ir guardando los cambios que vamos haciendo. Este **proceso de guardado** será el siguiente: 

1. El clásico `ctrl + s` o `cmd + s` en nuestro archivo del **VSC**.
2. Abrimos la **terminal**, comprobamos que nos encontramos en la ruta correcta y escribimos los siguientes comandos:
    1. `git add .`
    2. `git commit -m "mensaje"`
    3. `git push`

3. Vamos al navegador y comprobamos que se ha hecho correctamente en nuestro **Github**.


---

### Recursos 

- [Recurso 1](https://developer.mozilla.org/en-US/docs/Learn/JavaScript/Client-side_web_APIs/Manipulating_documents)

- [Recurso 2](https://plainjs.com/javascript/manipulation/)

- [Recurso 3](https://eloquentjavascript.net/14_dom.html)

- [Recurso 4](https://eloquentjavascript.net/15_event.html)



